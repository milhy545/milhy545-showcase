# Archive Structure

This archive is intentional.

- `museum/` contains curated public-facing exhibits about architectural evolution.
- `snapshots/` contains preserved historical files and older public states.

The `museum` is part of the portfolio narrative.
The `snapshots` folder is raw historical material.
